// Copy to config.js (or run scripts/gen-xcconfig.sh, which writes it). The anon key is the client key;
// round_preview is the only thing it can read without a session.
window.XIX = { url: "https://zsipifaskpupjwfiinen.supabase.co", anonKey: "" };
